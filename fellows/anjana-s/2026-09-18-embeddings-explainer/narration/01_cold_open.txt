Hello, Anjana here. A large language model does not read words. It reads numbers. Every token that enters the model gets converted into a long list of numbers called a vector. That vector is the embedding. And what it stores is more interesting than you think.