api-only surfaces — not called during capture, no screen to show
=================================================================
Source: medhavi-hub DEVELOPER.md at efcc3f5 (sections 4, 5.1, 8.6, 8.7).

access-verify        POST /api/access/verify   (+ OPTIONS)  — called by a textbook site's middleware
                     with ?access_token; sets textbook_session cookie on success. Not exercised:
                     textbook sites currently 307 back to the hub with a localhost blocked_url.
memory-api           /api/memory/*  — service-to-service, requires MEMORY_API_SECRET header.
                     Not called. Secret never read or shown.
articles-api         GET /api/articles/*  — public, CDN-cached glossary definitions. Not called.
cors-auto-whitelist  Registering a textbook URL whitelists its origin for CORS (lib/textbook-manager.ts).
                     Source fact; nothing to observe in the admin UI.
