But static embeddings are just the start. In a large language model, the same token gets a different vector depending on context. The word bank next to river produces one embedding. Bank next to earnings produces a completely different one. That is what the transformer layers do. They take the initial embedding and reshape it based on every other token in the sequence. By the final layer, the vector no longer represents just a word. It represents that word's meaning in this specific sentence.