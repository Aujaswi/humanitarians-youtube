# SHOTLIST — no-face-no-problem/short (9:16 Shorts cut)

Derived from the parent 16:9 reel via shorts.py. Parent is 4:11.1, over the
3:00 Shorts cap, so shorts.py auto-dropped 3 beats (longest/most-skippable
middle beats first, hook/hero/handoff/outro protected): B02 (the strategy),
B03 (the voice gap), and B07 (the closing reflection). The rewritten outro
(B09) names what got cut and points to the long.

## Composer/outro beats — rewired to portrait (916) compositions

B00 · ClaudeComposerAsk916 (Remotion, portrait re-render) — plain ask hook
B08 · ClaudeComposerAsk916 (Remotion, portrait re-render) — HANDOFF LAW
B09 · ClaudeTitleOutro916 (Remotion, portrait re-render) — outro NARRATION
      rewritten (only audio regenerated for this Short) to name the cut
      material and send viewers to the long; title/handle unchanged

## Manim GRAPHIC beats — portrait-relaid-out in short/scenes.py

B00B · B00B_AgrimaIntro — presenter card ("Hi, I'm Agrima." + lead-in),
       single-column portrait layout, unchanged content
B01   · B01_TheHook — stock-footage/caption card, motifs scaled to fit the
        frame (carries forward the parent's overflow-bug fix)
B04   · B04_WhereItPays — niche cards in a 2-column grid instead of a row
B05   · B05_TrustFinding — trust-stat card + plain-text skepticism flag
        (carries forward the parent's unicode-glyph fix), wrapped to fit
B06   · B06_TheReframe — watch-time meter, narrowed to fit the portrait
        column, same tags and crossed-out face icon

## Dropped from this Short (content lives only in the parent long)

B02 · B02_TheStrategy — the five-node flow (Topic -> Script -> AI Voice ->
      Stock Footage -> Post) and the "big share of new creator ventures"
      hedged stat
B03 · B03_TheVoiceGap — the before/after AI-voice comparison, the blind
      listening-test claim, and the detection arms-race side effect
B07 · B07_ClosingQuestion — the article's own closing typographic beat

## Endcard

END · silent branded card, ~4.5s, generated by shorts.py (endcard_png);
      handle set to @HumanitariansAI via --handle at plan time; if
      compiling at true 4K (--height 3840), regenerate END.png manually
      with Georgia (C:\Windows\Fonts\georgia.ttf) at 2160x3840 — shorts.py's
      find_serif() has no Windows path and silently falls back to PIL's
      tiny bitmap font otherwise (fix used on every prior short this
      session).

## Notes

- No pantry / archival stills used — every visual is either a Claude-skin
  Remotion portrait (916) beat or a from-scratch portrait Manim scene
  (short/scenes.py). No open pantry slots.
- Portrait Manim scenes require `config.frame_width = 4.5` /
  `config.frame_height = 8.0` set explicitly, with all text/cards guarded
  to the 1.95-half-width portrait safe band (manim_layout_audit.py
  --portrait: safe_w=1.95, safe_h=3.4) and generous buff values (>=0.7) on
  any to_edge() call.
- Only B09's narration was regenerated (Kokoro) — every other beat reuses
  the parent's mp3 unchanged, per THE SHORTS LAW.
- @HumanitariansAI used throughout, matching branding precedent.
