"""
Manim scenes for claude-sai-what-the-measure-was-hiding.

INTENTIONALLY EMPTY — this is an all-Remotion ai-explainer reel. Every beat is
a registered Remotion composition rendered by remotion_scenes.py, so there is
nothing here to declare. The file exists only to satisfy run.sh's guard, which
refuses a reel that has no scenes.py at all.

Do not add a Manim scene declaration to this file, not even inside a comment or
this docstring: the gate scanner reads text rather than code, so a declaration
appearing anywhere here makes GATE F believe a Manim beat is pending and demand
FACTCHECK / SHOTLIST / PROMPTS for it.
"""
