# PROMPTS — The Link That Pointed Nowhere

## Status: no pantry assets needed — all Manim

All 10 beats are self-contained Manim scenes in `scenes.py` (landscape) and
`vertical/scenes.py` (the native-portrait companion) — no vox stills, no
Remotion components, no generated/photographic images. There is no pantry
shopping list for this reel (see `beat_sheet.json`'s
`metadata.gates.shopping_list`: "N/A — all beats are self-contained Manim
scenes (scenes.py); no pantry stills or Remotion assets needed"). This file
is kept as the required paperwork-set placeholder (GATE F) and as a record
that no image-generation prompts were ever needed, for either the landscape
master or the vertical companion.

Every on-screen artifact (the feed-item click hook, the regex/real-link
mismatch, the resolution flow diagram with its caveat label, the two
call-order diagrams, the three verification rounds and resolved domains, the
two honest-limits cards and the visibility log line) is authored directly as
Manim `Text`/`MONO` mobjects, quoted verbatim from
`/Users/pranavijs/mycroft/scripts/regulatory-intel/B3-VERIFICATION.md` —
see `SOURCES.md`'s claim -> source mapping.
